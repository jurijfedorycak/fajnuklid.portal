# Test FTP

- skotnicky.sk:21
- fajnuklid.skotnicky.sk
- Gh37=oaW2\_

## Environment config

`api/.env` is maintained manually on the server — it is git-ignored and excluded from the CI FTP deploy, so any config changes (DB, JWT, R2, SMTP, etc.) must be applied on the server directly.
