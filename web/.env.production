# Production build (`vite build`) reads this file. The URL is hard-baked
# into the dist/ bundle, so set it to the deployed API host before building
# the native app. The same dist/ ships to both the web at portal.fajnuklid.cz
# and the iOS/Android stores.
VITE_API_URL=https://portal.fajnuklid.cz/api
