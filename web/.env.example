# Dev-server reads this. For production builds vite picks up .env.production,
# whose VITE_API_URL must be an absolute HTTPS URL — relative paths break
# under the Capacitor WebView (origin is https://localhost, not your domain).
VITE_API_URL=http://localhost:8001
